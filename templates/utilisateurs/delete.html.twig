{{ form_start(form, {'action': path('utilisateurs.delete')}) }}
    <div class="row d-flex justify-content-center">
        <div class="m-2">
            {{ form_widget(form.close) }}
        </div>
        <div class="m-2">
            {{ form_widget(form.submit) }}
        </div>
    </div>
{{ form_end(form) }}